services:
  app:
    build: .
    container_name: LaravelApp
    volumes:
      - ./backend:/var/www/html
    ports:
      - "8000:80"

  frontend:
    image: node:26.2.0
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"